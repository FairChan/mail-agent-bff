# 2026-04-22

## Drawer Outside-Close + Right-Retract Animation

- Optimized the global right-drawer stack in `apps/webui` so clicking blank space outside the active drawer now closes only the topmost drawer.
- Kept the existing backdrop-close path and added an outside-panel pointer handler in the drawer host, so the behavior also works when users click non-panel whitespace.
- Added explicit close animations in `apps/webui/src/styles.css` so drawer exit now fades the backdrop and retracts the panel back to the right instead of disappearing abruptly.
- Extended the Playwright smoke to reopen the knowledge drawer, click the blank page area, assert the closing state appears, and confirm the drawer fully disappears.
- Validation passed:
  - `rtk npm --workspace apps/webui run check`
  - `rtk git diff --check -- apps/webui/src/components/drawer/GlobalDrawerHost.tsx apps/webui/src/components/drawer/EventClusterDetailDrawer.tsx apps/webui/src/components/drawer/PersonProfileDetailDrawer.tsx apps/webui/src/styles.css apps/webui/e2e/smoke.spec.ts`
  - `rtk npm --workspace apps/webui run test:e2e -- --grep "knowledge-base|source changes|context mail pool"`
  - `rtk npm --workspace apps/webui run build`
- Independent audit status:
  - Attempted a read-only Codex audit for this change, but the run hit the Codex usage limit before producing findings.
  - Blocker evidence is recorded at `.harness/audit/2026-04-22-drawer-outside-close-animation-audit.md`.

## Personalization Feedback Loop

- Completed the behavior-driven personalization loop for the mail agent.
- Added persisted feedback/override/learned-signal storage, local readable personalization artifacts, and resolved runtime profile application across triage and knowledge-base surfaces.
- Added right-top quadrant override controls to mail/event/person detail drawers and mail detail modal/page surfaces.
- Added detail dwell tracking, related-mail open tracking, external original-mail open tracking, calendar-sync tracking, and knowledge-card feedback through the BFF.
- Important audit fixes:
  - reset-to-auto no longer records a positive `manual_override` learning event;
  - knowledge-card save is not double-counted by frontend and backend;
  - delayed dwell feedback now uses the source active when the detail window opened;
  - inbox normalization no longer applies personalization twice.
- Validation passed: BFF/WebUI check and build, harness semantic, and targeted diff check.
- Applied the local Prisma migration with `npm run db:migrate:deploy` and confirmed BFF health reports `prisma.ok=true`.
- `check:standard` still has the known two new-mail processing workbench Playwright drifts around `立即处理新邮件`; unrelated to this personalization work.
- Audit evidence: `.harness/audit/2026-04-22-personalization-feedback-loop-audit.md`.
