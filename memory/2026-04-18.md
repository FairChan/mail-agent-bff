# 2026-04-18

## Notification Center Integration

- Continued the post-KB/product-completion track by wiring the existing notification poll backend into the main WebUI header.
- Added shared notification payload types, source-aware notification parsing/polling in `MailContext`, and stale-response guards so a late poll cannot repaint a different mailbox after source switch.
- Rebuilt the small `NotificationCenter` into a real popover for urgent-important mail plus the daily digest, with a manual sync action.
- Updated smoke coverage to use the real `/api/mail/notifications/preferences` envelope, assert `/api/mail/notifications/poll` query parameters, and verify the urgent item + digest render path.
- Validation passed: shared-types typecheck, WebUI/BFF checks, WebUI/BFF builds, `npm run harness:smoke` (`8/8`), `git diff --check`, and `npm run check:standard`.
- Final sub-agent audit (`Bernoulli`, `gpt-5.4-mini`) reported no Critical/High/Medium/Low findings; evidence stored in `.harness/audit/2026-04-18-notification-center-audit.md`.

## Realtime Notifications And Calendar Confirmation

- Pushed the next product step after the notification center: connected the existing SSE notification stream into `MailContext` so the active mailbox now receives realtime snapshot updates instead of relying only on manual poll.
- Added browser-level desktop notifications for urgent mail and daily digests, with local dedupe keyed by source + message/date.
- Turned `processingResult.calendarDrafts` into a real confirmation loop in the Inbox workbench: each draft can be written individually, and the full set can be batch-synced into Outlook calendar from the same panel.
- Removed the old behavior where a calendar sync failure could flip the whole Inbox into the global error display.
- Validation passed again: shared-types/WebUI/BFF checks, WebUI/BFF builds, `npm run harness:smoke` (`8/8`), `git diff --check`, and `npm run check:standard`.
- Final sub-agent audit (`Maxwell`, `gpt-5.4-mini`) reported no Critical/High/Medium/Low findings; evidence stored in `.harness/audit/2026-04-18-realtime-notification-calendar-loop-audit.md`.

## Notification Settings And Digest Scheduling

- Turned the placeholder notification settings section into a source-aware control surface for urgent push, daily digest time, and digest timezone.
- Fixed two rounds of audit findings: late preference responses can no longer repaint after source changes, and the settings form no longer lets a user save stale values into a newly selected mailbox before the fresh preferences load.
- Upgraded smoke coverage to keep per-source notification preference state, assert `sourceId` on notification preference GET/POST calls, and verify two mailbox sources preserve different settings after switching.
- Validation passed: shared-types/WebUI/BFF checks, WebUI/BFF builds, `npm run harness:smoke` (`10/10`), `git diff --check`, and `npm run check:standard`.
- Final sub-agent audit (`Arendt`, `gpt-5.4-mini`) reported no findings; evidence stored in `.harness/audit/2026-04-18-notification-settings-digest-schedule-audit.md`.

## Local Test Deployment

- Restarted the local preview stack for manual testing after the notification-settings work.
- Main WebUI is available at `http://127.0.0.1:4173`, the standalone mail agent window is `http://127.0.0.1:4173/?window=agent`, and the BFF is `http://127.0.0.1:8787`.
- Injected an in-process `APP_ENCRYPTION_KEY` when starting the BFF so Microsoft direct auth is available for this local run.
- Current `/health` still reports degraded because Prisma auth storage is disabled (`prisma.ok=false`), but LLM and Microsoft auth are up for local product testing.

## Onboarding, Outlook, And Unprocessed Queue

- Persisted `APP_ENCRYPTION_KEY` into the ignored `apps/bff/.env` so local BFF restarts no longer depend on a one-off injected key.
- Added a dedicated `unprocessed` mail quadrant end-to-end and rewired live triage to prefer KB-backed processed state, so unclassified mail no longer falls into `不紧急不重要`.
- Sped up historical summary generation by shortening analysis payloads, enforcing terse JSON completions, and parallelizing the lightweight summary batches used by the server-side inbox summarizer.
- Improved Microsoft direct auth UX by widening the popup postMessage/close window and activating connected sources optimistically before background routing verification finishes.
- Added the left-nav `Agent Window`, removed the floating `Open Agent` widget, and introduced a first-login tutorial view that exposes local KB artifact files and summary onboarding.
- Expanded smoke coverage to `11/11`, including a first-login tutorial regression.
- Independent audit evidence for this slice is stored in `.harness/audit/2026-04-18-onboarding-outlook-unprocessed-audit.md`; final backend/frontend re-audits both reported `No findings`.

## KB Source-Pinned Documents And Summary Routing

- Continued the onboarding/documents work after the user asked to continue and later interrupted a long `check:standard` run.
- Moved historical KB summarization and single-mail summarization onto `LlmGatewayService`, fixing the audit finding that the old path bypassed user/source LLM routes and usage accounting.
- Exported KB documents are now refreshed after new-mail processing, and export failures surface as partial processing instead of silent success.
- File-backed KB reads and artifact previews no longer require live mailbox routing readiness; backfill trigger still requires a routable mailbox.
- Frontend source isolation was tightened: KB slices clear on source switch, `fetchSources()` ignores stale responses, documents/tutorial artifact fetches are source-pinned and stale-response guarded, and document refresh smoke coverage no longer depends on dev-mode double effects.
- Validation passed after the final fixes: BFF check, WebUI check, WebUI smoke `14/14`, and `git diff --check`.
- Audit evidence stored in `.harness/audit/2026-04-18-kb-source-pinned-docs-audit.md`. Final backend sub-agent audit returned `No findings`; final frontend re-audit after the last fix was blocked by Codex usage quota, but all returned Critical/High findings and the last returned Medium finding were fixed.
