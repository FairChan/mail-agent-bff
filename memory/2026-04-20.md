# 2026-04-20

## 17:55 Asia/Shanghai - Industrialization Cleanup

- Continued the repo industrialization plan as a Code task.
- Added a repo-boundary guard (`scripts/check-repo-boundary.mjs`) and wired it into `check:standard` so runtime mailbox data, generated outputs, legacy prototypes, imported reference snapshots, coverage, and local caches cannot remain tracked.
- Removed tracked runtime/private data from git while preserving local `apps/bff/data`, and deleted stale generated or retired structures such as `website-login-main`, `reference/remote-webui-2026-04-19`, root `mail-kb`, Playwright/test result artifacts, `.DS_Store`, and `*.tsbuildinfo`.
- Tightened BFF production config and Kubernetes manifests around real persistence, Redis auth sessions, Prisma auth, encryption, Microsoft OAuth, port `8787`, and supported health probes.
- Centralized mail-body HTML sanitization with DOMPurify and hardened links.
- Sub-agent audit was blocked by quota, so an external SiliconFlow `Pro/zai-org/GLM-5.1` independent audit reviewed code/config diffs and deletion categories only. It passed with no Critical/High findings; low-risk notes led to adding `<b>` tag support and coverage repo-boundary checks.
- Validation passed: BFF check, WebUI check, repo-boundary check, harness semantic, and full build.

## 18:15 Asia/Shanghai - Remote Mail Privacy Review

- Fetched `origin/master` and reviewed the latest three mail privacy commits in a detached worktree without merging into the dirty local workspace.
- The privacy layer is broadly wired into BFF-to-LLM paths, but two Medium gaps remain: Microsoft Graph recipient paths like `toRecipients[].emailAddress.address` are still email-shaped aliases instead of `[recipient-email]`, and free-text email addresses are not stripped because `stripMailAddresses()` returns the original input.
- Low hygiene issue: Mastra prompt instructions still expose raw `userId` and `sourceId` to the LLM.
- The guide references `deploy/docs/MAIL_PRIVACY_DELIVERY.md`, but that file is absent from fetched `origin/master`.

## 18:48 Asia/Shanghai - Mail Privacy Integrated And Hardened

- Synced the remote mail privacy feature files from `origin/master` into the local workspace without merging unrelated branch state.
- Fixed Graph recipient address stripping, free-text email stripping, and removed raw `userId` / `sourceId` from the Mastra prompt.
- Added the missing `deploy/docs/MAIL_PRIVACY_DELIVERY.md`.
- Local synthetic privacy assertions passed for flat fields, Graph-shaped fields, and fail-closed behavior when the HMAC key is missing.
- Prisma validate, BFF typecheck, BFF build, and repo-boundary checks passed.
- Independent sub-agent audit (`Lovelace`) returned clean with no findings; only residual gap is that the live smoke was not executed because it makes an external LLM call.

## 20:03 Asia/Shanghai - Local Test Deployment

- Confirmed the local WebUI preview for this repo is available at `http://127.0.0.1:4173/`.
- Confirmed port `5173` belongs to another project and is not the mail-agent entry.
- Restarted the BFF so port `8787` now serves the latest built backend with the new mail privacy logic.
- Health check is green after restart.

## 20:22 Asia/Shanghai - Admin Login Error Hardened

- Fixed the frontend auth/mail API wrappers so empty or non-JSON backend responses no longer explode with `Unexpected end of JSON input`; they now surface a human-readable fallback error.
- Added shared helper `apps/webui/src/utils/http.ts` and rewired `App.tsx`, `AuthContext.tsx`, `MailContext.tsx`, and `utils/api.ts` to use it.
- Added explicit `preview.proxy` parity in `apps/webui/vite.config.ts`, then rebuilt WebUI and BFF.
- Relaunched local services in detached `screen` sessions:
  - `mail-agent-webui` serves `http://127.0.0.1:4173/`
  - `mail-agent-bff` serves `http://127.0.0.1:8787/`
- Verified admin login succeeds at `4173` with `admin@true-sight.local / MeryAdmin2026!`, and wrong-password responses stay structured (`401`) instead of crashing the UI parser.
- Independent sub-agent audit (`Bernoulli`) came back clean.

## 21:23 Asia/Shanghai - Multi Mail Provider Architecture

- Added a provider-registry based multi-mail architecture: Outlook stays on Microsoft Graph, while Gmail, iCloud Mail, 163, QQ, Aliyun, and custom mailboxes now share a new IMAP onboarding/runtime path.
- Added encrypted IMAP credential persistence (`MailSourceCredential`), IMAP inbox/detail adapters, authenticated `GET /api/mail/providers`, and authenticated `POST /api/mail/connections/imap`.
- Added WebUI multi-mail settings so users can discover provider defaults and connect IMAP-backed accounts from the product shell.
- After audit, fixed two real High issues: mail-body cache is now source-scoped and cleared on source switch, and IMAP onboarding is now TLS-only end to end.
- Also hardened source deletion to clean stored IMAP credentials and bound credential ownership to `(sourceId,userId)` at the Prisma/schema layer.
- Validation passed: shared-types typecheck, BFF/WebUI checks, Prisma validate, BFF/WebUI builds, authenticated `/api/mail/providers`, and live `imapSecure=false` rejection with `IMAP_TLS_REQUIRED`.
- Full `npm run check:standard` still fails in pre-existing WebUI smoke tests that assume older UI contracts; multi-mail files themselves validated cleanly.
- Independent sub-agent audit (`Locke`) ended with no remaining Critical/High findings; one Medium IMAP list-performance improvement was deferred to 2026-04-23.

## 23:57 Asia/Shanghai - Gmail Direct OAuth Connected

- Completed the Gmail direct OAuth path on top of the multi-mail architecture: BFF now owns Google popup start/callback routes, persists tenant-scoped Gmail OAuth accounts, and reads Gmail inbox/detail through the Gmail API when a source uses `connectionType=gmail_oauth`.
- Added local DB migration `202604201930_google_accounts` and applied it successfully with `npm run db:migrate:deploy`.
- WebUI now exposes Gmail direct connect in both Settings and the sidebar account modal, while IMAP remains the fallback path for Gmail app-password onboarding.
- Audit surfaced two medium issues and two low issues; all were fixed in the same session. The useful lesson was to check the entire auth lifecycle, not just the happy path: start-route readiness, callback payload ergonomics, and session-teardown cleanup all matter.
- Final validation stayed green on BFF/WebUI typecheck and build, Prisma validate, health, authenticated provider catalog, and Gmail direct-start smoke. `npm run check:standard` is still failing in one pre-existing Playwright smoke unrelated to this Gmail slice.
