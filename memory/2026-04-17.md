# 2026-04-17

## Microsoft Outlook Direct Auth

- Fixed the Outlook direct-auth callback bug where Microsoft redirected to `localhost:8787` and the BFF global `/api/*` auth hook returned raw JSON `{"ok":false,"error":"Unauthorized"}` before the callback route could validate OAuth `state`.
- Root cause: the OAuth popup starts from `127.0.0.1:5173`, but Microsoft callback uses `localhost:8787`; callback cannot rely on the host-only browser cookie and must rely on one-time OAuth `state` tied to the original session.
- Code changes:
  - Allowed `/api/mail/connections/outlook/direct/start` and `/api/mail/connections/outlook/direct/callback` through the global auth hook.
  - Kept start route protected with route-level session validation and rate limiting.
  - Added `touchAuthSessionForRequest()` so direct auth reuses Redis hydration/revocation behavior.
  - Added pre-token-exchange and pre-token-persist session checks for Microsoft callback.
- Validation passed: BFF check/build, targeted semantic check, health checks, callback HTML behavior, authenticated start redirect, and `npm run harness:smoke`.

## Email Verification Registration

- Implemented the active registration flow in `apps/bff/src/server.ts`: register now creates an in-memory pending registration, sends/logs a 6-digit verification code, and only creates the real user after `/api/auth/verify` succeeds.
- Added `/api/auth/resend` with cooldown and rate limiting, and bounded/expired pending-registration cleanup.
- Wired WebUI registration into a verify step using `AuthContext` and `ContextAuthScreen`.
- Configured ignored local Gmail SMTP defaults for `mery.secretary@gmail.com` using `smtp.gmail.com:465`, but kept `SMTP_ENABLED=false` until the user provides a Gmail App Password or OAuth refresh token.
- Sub-agent audit found no Critical/High issues. Deferred medium: pending registrations are memory-only and need Redis/DB persistence before production/multi-instance use.
- Validation passed: BFF/WebUI check/build, targeted semantic, runtime register -> log code -> verify -> authenticated session, smoke 10/10, and `check:standard`.

## Standalone Agent Window

- Added a dedicated `Mail Copilot` window at `/?window=agent` so the user can chat with the mailbox agent in a full-page workspace instead of only using the floating panel.
- Added header/floating-panel entrypoints, shared SSE conversation state, source selection/verification, capability loading, and standalone smoke coverage.
- Audit-driven fixes included clearing stale drafts on source/thread reset, hydrating assistant output from final SSE answers, and restoring compact-panel tool progress feedback.
- Local accessible WebUI dev server is currently running on `http://127.0.0.1:4173`, with BFF on `http://127.0.0.1:8787`.

## Historical Mail KB Pipeline

- Rebuilt the old-mail knowledge-base flow around the new file-backed KB store instead of the old Prisma-only route.
- The agent can now trigger a 30-day mailbox-history backfill, monitor the KB job in the standalone agent window, and later search persisted historical summaries.
- Exported KB artifacts are tenant-isolated and include mail IDs, subject index, score index, mail summaries, event clusters, and sender profiles.
- Fixed audit issues around premature export completion, full-KB search coverage, Markdown export sanitization, source-pinned stats, snapshot/SSE log merge safety, legacy score display, failure-phase UI, and batch-to-batch event/sender reuse.
- Current local access points remain:
  - WebUI: `http://127.0.0.1:4173`
  - Agent window: `http://127.0.0.1:4173/?window=agent`
  - BFF: `http://127.0.0.1:8787`

## Eisenhower Matrix And New-Mail Processing

- Added an Eisenhower four-quadrant matrix to the knowledge-base overview, with runtime quadrant fallback, score-scale handling, and smoke coverage for unknown quadrant / legacy score imports.
- Added `POST /api/mail/processing/run` as a single visible new-mail processing entrypoint: it updates the KB, checks urgent items/digest state, returns triage counts, and extracts calendar-ready drafts.
- Added an Inbox workbench panel with a manual "立即处理新邮件" action and partial-result status display.
- Audit-driven fixes removed duplicate real-mailbox refetching after processing, preserved partial results when later stages fail, and cleared processing state on source switch/failure.
- Latest validation passed: BFF/WebUI checks and builds, `npm run harness:smoke` with 7/7, `git diff --check`, and `npm run check:standard`.
