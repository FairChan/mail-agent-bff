# Required production secrets. Do not commit a real .env file.
PUBLIC_BASE_URL=https://mail.example.com
DB_PASSWORD=replace-with-strong-postgres-password
BFF_API_KEY=replace-with-at-least-16-random-characters
APP_ENCRYPTION_KEY=replace-with-32-plus-random-characters

# OpenAI-compatible provider used by the server-side LLM gateway.
LLM_PROVIDER_BASE_URL=https://api.openai.com/v1
LLM_PROVIDER_API_KEY=replace-with-provider-key
LLM_PROVIDER_MODEL=gpt-4.1-mini

# Microsoft Direct OAuth / Graph.
MICROSOFT_CLIENT_ID=replace-with-azure-app-client-id
MICROSOFT_CLIENT_SECRET=
MICROSOFT_TENANT_ID=common
MICROSOFT_REDIRECT_URI=
MICROSOFT_SCOPES="openid profile email offline_access User.Read Mail.Read Calendars.ReadWrite"

# Optional published ports.
WEBUI_PORT=8080
BFF_PORT=8787
REDIS_KEY_PREFIX=mail_agent:bff
